The *.npy files are saved versions of the generated features so we don't need to run those multiple times on the same set of input data.

The code for the baseline approach with the MLP is in baseline.ipynb

The code for the XGBoosted model is in xgboost2.ipynb